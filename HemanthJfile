pipeline {
    agent any
    environment {
        PATH = "/opt/maven/bin:$PATH"
    }
    // tools {
    //     maven 'maven3.8'
    // }
    stages {
        stage('MASTER -  Git-CLonce/Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/devopsjune2024/marchjava-app.git'
            }
        }
        stage('Maven-Package') {
            steps {
                sh 'mvn clean install'
            }
        } 
        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
 stage('docker') {
            steps {
              script{
                  sh 'docker login -u meghanasr -p dckr_pat_nXkHF0UTHYOmOSA0Jn_g4IaLHLk'
                  sh 'docker build -t mvn:v1 .'
                  sh 'docker tag mvn:v1 meghanasr/maven:v1'
                  sh 'docker push meghanasr/maven:v1' 
              }
            }
     }           
            
        } 
    }


